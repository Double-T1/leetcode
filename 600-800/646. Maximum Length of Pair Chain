var findLongestChain = function(pairs) {
    pairs.sort((a,b) => a[1]-b[1])
    let prev = pairs[0][1]
    let count = 1
    for (let i=1; i<pairs.length; i++) {
        if (prev < pairs[i][0]) {
            prev = pairs[i][1]
            count++
        }
    }
    return count
};
